# 当前适合我的算法解题框架与思路整理

##  我当前最核心的问题

我现在的主要问题不是完全不会想思路，而是：

**我能想到“这题为什么这样做”，但还不太能稳定地把这个道理翻译成“代码应该按什么顺序写”。**

所以我当前最该练的，不是盲目追求独立秒题，而是下面三个能力：

1. 把**题目本质**和**代码顺序**分开  
2. 把“变量角色”提前想清楚  
3. 写完后有一套固定的**自检方法**

---

##  我当前最适合的解题框架

以后每道题，先按下面的顺序写。

### 第一层：题目到底要我返回什么

先只写一句话。

例如：

- Two Sum：返回两个下标
- Valid Anagram：返回 `True / False`
- Merge Intervals：返回合并后的区间列表

这一层的作用是：

**先确定答案长什么样。**

---

### 第二层：这题的主路线是什么

这一层不写代码，只写核心策略。

例如：

- 哈希题：为了快速判断某个值是否出现过，所以用字典 / 集合
- 双指针题：为了同时从两边或两个位置推进，所以用左右指针
- 区间题：为了稳定比较相邻区间关系，所以通常先排序，再从左到右扫描

这一层的目标是：

**先决定解题路线，不要一上来就写语法。**

---

### 第三层：我要维护哪些变量

这一层是从“想法”过渡到“代码”的桥。

例如：

#### Valid Anagram
- `count`：记录每个字符出现次数

#### Merge Intervals
- `intervals`：排序后的原始区间列表
- `result`：已经整理好的合并结果
- `current`：当前正在处理的区间

#### 双指针题
- `left`
- `right`

这一层的目标是：

**提前想清楚，谁存答案，谁做比较，谁负责更新。**

---

### 第四层：代码顺序怎么排

这一层是我当前最需要练的部分。

以后先强行写成这种结构：

1. 先处理什么  
2. 再初始化什么  
3. 再循环什么  
4. 循环里分几种情况  
5. 最后返回什么  

例如 `Merge Intervals`：

1. 先处理空输入
2. 再排序
3. 再初始化 `result`
4. 再从第二个区间开始遍历
5. 循环里分“重叠 / 不重叠”两种情况
6. 最后返回 `result`

这一层的目标是：

**把思路翻译成执行顺序。**

---

### 第五层：有哪些特判 / 易错点

这一层单独列，不要混在主思路里。

例如 `Merge Intervals`：

- `intervals` 可能为空，所以要先判断
- `result[-1]` 是最后一个区间，不是最后一个数字
- `result[-1][1]` 才是最后一个区间的右边界
- 重叠时只更新右边界，因为已经按左边界排序
- `append(current)` 是把整个区间加入结果

例如 `Valid Anagram`：

- 长度不等，直接 `False`
- `count[ch] += 1` 前要先考虑键是否存在
- 第二个循环中既要检查 `ch not in count`，也要检查减完后是否小于 0

这一层的目标是：

**把小细节和主线分开，减少混乱。**

---

##  我以后不要再直接写大段 prose

我现在更适合先写固定模板，而不是一上来写一大段完整描述。

以后每道题，先写这 6 句：

```text
1. 题目要我返回什么？
2. 这题主路线是什么？
3. 需要维护哪些变量？
4. 代码顺序是什么？
5. 易错点是什么？
6. 写完后我怎么检查它对不对？
```

---

## 我当前最适合的代码前思考模板

在真正写代码前，先快速回答下面几个问题：

### 1）返回值类型
- 布尔值？
- 整数？
- 下标数组？
- 新数组？
- 新链表？

### 2）主工具
- 哈希表？
- 双指针？
- 排序？
- 栈？
- BFS / DFS？

### 3）核心变量
- 谁存答案？
- 谁表示当前元素？
- 谁记录边界？
- 谁记录状态？

### 4）代码顺序
- 先特判什么？
- 先初始化什么？
- 再进入什么循环？
- 循环里分什么情况？

### 5）最后返回什么
- 返回的是最终答案吗？
- 还是中间变量？
- 还是边界值？

---

##  我当前最适合的代码骨架模板

以后很多题都可以先往这个模板里套：

```python
def solve(...):
    # 1. 特判
    if ...:
        return ...

    # 2. 初始化
    ...
    ...

    # 3. 主循环
    for ... in ...:
        if ...:
            ...
        else:
            ...

    # 4. 返回答案
    return ...
```

我现在不需要追求花哨，而是要先建立：

**稳定的代码顺序感。**

---

##  写完代码后的固定自检模板

我现在写完以后不太敢确定是否正确，所以必须建立固定自检。

以后每道题写完后，问自己这 5 个问题：

### 1）我有没有先处理会导致后面报错的情况
例如：
- 空列表
- 空字符串
- 长度不够

### 2）我维护的核心变量是什么，它一直代表什么
例如：
- `result` 表示已经处理好的合并结果
- `count` 表示字符剩余次数

### 3）循环里是不是把所有情况分完了
例如：
- 重叠 / 不重叠
- 找到 / 没找到
- 相等 / 不相等

### 4）每种情况下变量更新后还合理吗
例如：
- 合并时为什么只改右边界
- 减到负数为什么就错

### 5）最后 `return` 的东西是不是题目要的
例如：
- 返回的是结果数组
- 不是循环变量
- 不是某个边界值
- 不是中间状态

---

##  为什么我会觉得数学和编程不一样

因为数学里常常是：

- 先想到整体证明结构
- 再慢慢补细节

但算法代码不是这样。

算法代码有一个很强的特点：

**顺序非常敏感。**

例如：

- 不先判空，后面就可能报错
- 不先排序，后面逻辑就不成立
- 不先初始化，后面变量就没法用

所以算法和数学的一个重要区别是：

**数学允许先在高层逻辑上成立，细节以后补；代码必须在执行顺序上也成立。**

这就是我现在最该练的能力。

---

## 我当前阶段最实用的 8 个算法 tips

### tip 1：先问“最后交什么”
先想清楚最后返回的是什么，再想过程。

---

### tip 2：先问“这题靠什么工具做”
哈希、双指针、排序、栈、递归……先选路线。

---

### tip 3：先写变量角色
例如：
- `count`
- `left / right`
- `result / current`

---

### tip 4：把代码强行分成三段
- 循环前：特判、初始化、排序
- 循环中：判断条件、更新变量
- 循环后：返回答案

---

### tip 5：小细节单独列
不要把它们混进主思路里。

---

### tip 6：先写骨架，再补判断
不要一口气写完整题。

先写：

```python
def xxx(...):
    if ...:
        return ...

    ...
    for ... in ...:
        pass

    return ...
```

然后再逐步补。

---

### tip 7：每写完一小段，就翻译回中文
例如：

```python
if not intervals:
    return []
```

立刻翻译成：

“如果区间列表为空，就直接返回空列表。”

这样能帮助我建立“思路 ↔ 代码”的稳定对应。

---

### tip 8：不要追求“看懂就会写”
看懂只是第一步。

我现在真正要练的是：

**能不能把“看懂”稳定地翻译成“写对”。**

---

##  我当前最适合的练习方式

我现在不适合：

- 直接完全独立硬写
- 只看答案不自己复现
- 一道题只写 prose 不碰代码

我当前最适合的流程是：

### 第一步
先写 6 句框架：
- 返回什么
- 主路线
- 变量
- 顺序
- 易错点
- 自检方式

### 第二步
看最小代码骨架

### 第三步
自己复现整题

### 第四步
写出每一段代码对应的中文意思

### 第五步
最后做 5 问自检

---

##  适合我当前阶段的最终总结

我现在最需要建立的，不是“完全独立秒题”的能力，而是：

1. 能先看清题目本质  
2. 能先定主路线  
3. 能提前想清楚变量角色  
4. 能把思路翻译成代码顺序  
5. 能写完后用固定模板自检  

以后我每道题都优先按下面这个最简模板走：

```text
1. 题目要我返回什么？
2. 这题主路线是什么？
3. 需要维护哪些变量？
4. 代码顺序是什么？
5. 易错点是什么？
6. 写完后我怎么检查它对不对？
```

这就是我当前阶段最适合的算法解题框架。
