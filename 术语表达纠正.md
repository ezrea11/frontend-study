# 编程术语表达纠正笔记：数组、对象、DOM

## 1. 为什么要区分术语

写代码时，很多日常说法能帮助自己理解，但在笔记、复述、面试表达中，需要更准确。

核心原则：

```text
先说它在代码结构中是什么，再说它在业务中代表什么。
```

例如：

```js
const users = [
  { name: "Alex", role: "Admin", active: true },
  { name: "Mia", role: "Editor", active: false }
];
```

更准确的说法是：

```text
users 是一个用户数组。
数组中的每一项是一个对象。
从业务上看，每一项表示一个用户，所以可以称为用户对象。
```

---

## 2. 数组里的“元素”是什么

“元素”是数组层面的通用叫法。

```js
const names = ["Alex", "Mia", "Leo"];
```

可以说：

```text
names 数组中的每一项是一个元素。
"Alex" 是 names 数组中的一个元素。
```

但如果是：

```js
const users = [
  { name: "Alex", role: "Admin", active: true },
  { name: "Mia", role: "Editor", active: false }
];
```

不能直接说：

```text
Alex 是 users 数组中的元素。
```

更准确是：

```text
users 数组中的第一个元素是一个用户对象：
{ name: "Alex", role: "Admin", active: true }

"Alex" 是这个用户对象的 name 属性值。
```

一句话记忆：

```text
数组最外层直接放什么，什么才是数组元素。
```

---

## 3. 元素、对象、用户对象的区别

以 users 为例：

```js
const users = [
  { name: "Alex", role: "Admin", active: true }
];
```

第一个数据：

```js
{ name: "Alex", role: "Admin", active: true }
```

它可以从三个角度称呼：

```text
数组视角：它是 users 数组中的一个元素。
类型视角：它是一个 object，所以可以说它是一个对象。
业务视角：它表示一个用户，所以可以说它是一个用户对象。
```

推荐表达：

```text
users 是一个用户数组，数组中的每一项是一个用户对象。
```

---

## 4. 什么是“类型视角”

类型视角就是：

```text
不看这个数据在业务里代表什么，只看它在 JavaScript 里是什么数据类型。
```

例如：

```js
const names = ["Alex", "Mia"];
```

```text
数组视角：names 数组里有两个元素。
类型视角：每个元素都是 string，所以可以叫字符串元素。
业务视角：这些字符串表示名字，所以可以叫名字字符串。
```

再比如：

```js
const scores = [90, 85, 100];
```

```text
数组视角：scores 数组里有三个元素。
类型视角：每个元素都是 number，所以可以叫数字元素。
业务视角：这些数字表示分数，所以可以叫分数值。
```

再比如：

```js
const users = [
  { name: "Alex" },
  { name: "Mia" }
];
```

```text
数组视角：users 数组里有两个元素。
类型视角：每个元素都是 object，所以可以叫对象。
业务视角：这些对象表示用户，所以可以叫用户对象。
```

---

## 5. 业务名词 + 数据类型

如果数组中的每一项有明确业务含义，可以使用：

```text
业务名词 + 数据类型
```

常见例子：

| 数据 | 推荐叫法 |
|---|---|
| `{ name: "Alex" }` | 用户对象 |
| `{ title: "Keyboard", price: 99 }` | 商品对象 |
| `{ id: 1001, status: "paid" }` | 订单对象 |
| `{ label: "Active", value: "active" }` | 状态对象 |
| `"active"` | 状态字符串 |
| `90` | 分数值 / 数字元素 |
| `true` | 布尔值元素 |

注意：

```js
const statuses = [
  { label: "Active", value: "active" },
  { label: "Inactive", value: "inactive" }
];
```

可以说：

```text
statuses 是状态数组，数组中的每一项是状态对象。
```

但如果是：

```js
const statuses = ["active", "inactive", "pending"];
```

更准确是：

```text
statuses 是状态数组，数组中的每一项是状态字符串。
```

不能说每一项是状态对象，因为它们不是 object。

---

## 6. forEach / filter 中的参数怎么称呼

代码：

```js
users.forEach(function (user) {
  console.log(user.name);
});
```

推荐表达：

```text
forEach 会遍历 users 数组中的每一个用户对象。
回调函数参数 user 表示当前正在处理的用户对象。
```

不要只说：

```text
forEach 操作 user。
```

更准确是：

```text
forEach 遍历数组。
每次遍历时，取出数组中的一个元素。
在 users 这个场景里，这个元素是一个用户对象，并临时命名为 user。
```

---

## 7. filter 的标准表达

代码：

```js
const activeUsers = users.filter(function (user) {
  return user.active === true;
});
```

推荐表达：

```text
filter 遍历 users 数组中的每一个用户对象。
如果当前用户对象的 active 属性严格等于 true，就保留这个用户对象。
filter 最终返回一个新数组 activeUsers。
activeUsers 只包含 active 为 true 的用户对象。
```

注意：

```text
filter 不会修改原来的 users 数组。
filter 会返回一个新的数组。
```

---

## 8. HTML、DOM、JavaScript 的关系

推荐记法：

```text
HTML：写在文件里的页面源码。
DOM：浏览器根据 HTML 解析出来的页面对象结构。
DOM 树：DOM 对象之间形成的父子层级关系。
JavaScript：通过 DOM API 读取和修改 DOM 树。
```

例如：

```html
<ul id="userList"></ul>
```

浏览器会把它解析成 DOM 树中的一个 `ul` 节点。

JavaScript 可以通过：

```js
const userList = document.querySelector("#userList");
```

获取这个 `ul` 对应的 DOM 元素对象。

---

## 9. DOM 节点和 DOM 元素对象

DOM 节点是总称。

DOM 元素对象是 DOM 节点中的一种，专门对应 HTML 标签。

例如：

```html
<ul id="userList">
  <li>Alex</li>
</ul>
```

解析后：

```text
<ul>      → DOM 节点，也是 DOM 元素对象
<li>      → DOM 节点，也是 DOM 元素对象
"Alex"    → DOM 节点，但不是 DOM 元素对象，而是文本节点
document  → DOM 节点，但不是普通 HTML 元素对象
```

一句话记忆：

```text
所有 DOM 元素对象都是 DOM 节点；
但不是所有 DOM 节点都是 DOM 元素对象。
```

---

## 10. DOM 操作中的标准说法

### querySelector

```js
const userList = document.querySelector("#userList");
```

推荐表达：

```text
通过 querySelector 从 DOM 树中获取 ul 对应的 DOM 元素对象。
```

不要说：

```text
JS 找到 HTML。
```

更准确是：

```text
JS 通过 DOM API 查询 DOM 树中的元素。
```

---

### createElement

```js
const li = document.createElement("li");
```

推荐表达：

```text
通过 createElement 创建一个新的 li DOM 元素对象。
```

注意：

```text
这个 li 此时还没有插入 DOM 树，所以页面上看不到。
```

---

### textContent

```js
li.textContent = user.name + " - " + user.role;
```

推荐表达：

```text
给 li 元素对象设置文本内容。
```

不要说：

```text
创建 li 列表。
```

因为 li 已经在上一行创建了，这一行只是设置文字内容。

---

### appendChild

```js
userList.appendChild(li);
```

推荐表达：

```text
把 li 这个 DOM 元素对象作为子节点追加到 userList 这个 ul 元素对象中。
```

也可以说：

```text
把 li 插入 DOM 树，使它显示到页面上。
```

不要说：

```text
把 li 的内容加到 userList 里。
```

因为添加进去的是整个 `li` 元素，不只是文字内容。

---

## 11. Day 2 代码的标准表述

代码：

```js
function renderUsers(list) {
  userList.innerHTML = "";

  list.forEach(function (user) {
    const li = document.createElement("li");
    li.textContent = user.name + " - " + user.role;
    userList.appendChild(li);
  });
}
```

推荐讲法：

```text
renderUsers(list) 是一个用户列表渲染函数。

list 表示传入的用户数组。函数执行时，首先通过 userList.innerHTML = "" 清空 ul 容器中已有的旧列表项，避免重复渲染。

然后使用 forEach 遍历 list 数组。每次遍历时，user 表示当前正在处理的用户对象。

在每次循环中，document.createElement("li") 会创建一个新的 li DOM 元素对象。随后通过 li.textContent 设置这个 li 的文本内容，显示当前用户的 name 和 role。最后通过 userList.appendChild(li) 把这个 li 元素追加到 ul 容器中，使其显示在页面上。
```

---

## 12. 常用表达对照表

| 日常说法 | 更准确说法 |
|---|---|
| users 里的东西 | users 数组中的每一项 |
| users 里的 user | users 数组中的一个用户对象 |
| Alex 是 users 里的元素 | Alex 是用户对象的 name 属性值 |
| filter 筛选 users | filter 遍历 users，并返回满足条件的新数组 |
| activeUsers 是 active 的 users | activeUsers 是由 users 筛选得到的新数组，只包含 active 为 true 的用户对象 |
| JS 找到 HTML | JS 通过 DOM API 查询 DOM 树中的元素 |
| 创建一个 HTML | 创建一个 DOM 元素对象 |
| li 加到页面里 | 将 li 插入 DOM 树并显示到页面上 |
| 清空页面 | 清空 ul 容器中的旧子元素 |
| 添加 li 的内容 | 给 li 设置文本内容 |
| 把 li 的内容加到 ul | 把 li 元素追加为 ul 的子元素 |

---

## 13. 最推荐的固定模板

以后解释数组时，可以用：

```text
xxx 是一个数组，数组中的每一项是一个 xxx 对象 / 字符串 / 数字。
当前遍历到的这一项临时命名为 xxx。
```

以后解释 DOM 时，可以用：

```text
HTML 提供固定容器。
JavaScript 通过 DOM API 获取这个容器对应的 DOM 元素对象。
然后根据数据动态创建新的 DOM 元素对象，并把它们追加到容器中。
```

以后解释 Day 2 这类列表渲染时，可以用：

```text
users 是用户数组，数组中的每一项是用户对象。
renderUsers 会遍历传入的用户数组，为每个用户对象创建一个 li DOM 元素对象，并把这些 li 追加到 ul 容器中。
```
